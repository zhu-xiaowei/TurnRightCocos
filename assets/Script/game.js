cc.Class({
    extends: cc.Component,

    properties: {
        outRect: {
            default: null,
            type: cc.Node
        },
        innerRect: {
            default: null,
            type: cc.Node
        }
    },

    // use this for initialization
    onLoad: function () {
        
        
    },

    // called every frame
    update: function (dt) {

    },
});
